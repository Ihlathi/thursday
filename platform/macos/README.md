# macOS adapter — future work
Implement the same shared ToolCall/ToolResult protocol using macOS Accessibility
and approved direct APIs. Core requires no OS branches. No macOS computer actions
are implemented by this scaffold. Use `mocks/platform.py` for development today.
